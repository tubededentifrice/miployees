[project]
name = "miployees-mocks"
version = "0.0.1"
description = "Disposable UI mocks for miployees — not production code."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "jinja2>=3.1",
    "itsdangerous>=2.2",
]
