// Shim for legacy imports. New code should import from "@/types" or
// the specific sub-module. This re-exports everything so existing
// `from "@/types/api"` paths keep compiling.
export * from "./index";
